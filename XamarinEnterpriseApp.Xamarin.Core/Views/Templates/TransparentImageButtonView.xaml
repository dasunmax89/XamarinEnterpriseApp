<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
        xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
        x:Class="XamarinEnterpriseApp.Xamarin.Core.Views.TransparentImageButtonView"
        HeightRequest="{StaticResource ButtonHeight}">
    <Grid  VerticalOptions="CenterAndExpand" HorizontalOptions="FillAndExpand"
            RowSpacing="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Image Grid.Column="0" x:Name="ButtonImageView" VerticalOptions="Center"
                HorizontalOptions="Center" Margin="10,0"
                BackgroundColor="Transparent">
        </Image>
        <Label Grid.Column="1" x:Name="CaptionLabel"
                Style="{StaticResource TransparentButtonLabelStyle}"
                VerticalOptions="Center" HorizontalOptions="FillAndExpand"
                LineBreakMode="WordWrap" Margin="5,0">
        </Label>
    </Grid>
</ContentView>